.errors
  - if obj.errors.any?
    .summary
      = "The form contains #{ pluralize(obj.errors.count, 'error') }:"
    .details
      %ul
        - obj.errors.full_messages.each do |err_msg|
          %li=err_msg
